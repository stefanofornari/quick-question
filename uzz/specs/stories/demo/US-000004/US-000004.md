# User Stories for Domain: Demo

## [US-000004] Provide a demo application with simple configuration UI
**As a** project evaluator or developer,
**I want** a demo JavaFX application
**So that** I can quickly verify how the component behaves

### Technical Context & Constraints
- **Target System:** JavaFX desktop application
- **Toolchain / Rules:** JavaFX, CommonsFX, AtlantaFX, Maven, JUnit 5, TestFX, AssertJ, headless testing, TDD workflow, snake_case test names, use `then()` for BDD assertions

### Acceptance Criteria
```gherkin
Scenario: Demo application starts with WebChat component and Provider selector
  Given the demo application is launched
  When the main window is displayed
  Then the user can see the component and a Provider selector

Scenario: Select and show Web Chat
  Given the application is launched
  When the user clicks on a different provider
  Then the component show the web chat site of that provider
```